#if($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#else
  #set($response = $ctx.result)

  #if($response.error)
    $util.error($response.error.message, $response.error.errorType)
  #else
    ## Retorna os itens normalmente
    $util.toJson($response)
  #end
#end
