schema {
    query: Query
    mutation: Mutation
}

type Query {
    getUsers: [User!]!
}

type Mutation {
    addUser(input: UserInput!): User!
}

type User {
    id: ID!
    name: String!
    email: String!
    status: Boolean!
    createdAt: AWSDateTime!
    updatedAt: AWSDateTime!
}

input UserInput {
    name: String!
    email: String!
    status: Boolean!
}
