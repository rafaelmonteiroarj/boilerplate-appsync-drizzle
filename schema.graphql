schema {
    query: Query
    mutation: Mutation
}

type Query {
    getUsers: [User!]!
}

type Mutation {
    addUser(input: UserInput!): User!
}

type User {
    id: ID!
    name: String!
    email: String!
    active: Boolean!
    isAdmin: Boolean!
    createdAt: AWSDateTime!
    updatedAt: AWSDateTime!
}

input UserInput {
    name: String!
    email: String!
    password: String!
}
